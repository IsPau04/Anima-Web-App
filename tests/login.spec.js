const { test, expect } = require('@playwright/test');

test.describe('Login (dummy)', () => {
  test('login válido', async () => {
    expect(true).toBe(true);
  });

  test('login inválido', async () => {
    expect(true).toBe(true);
  });
});
