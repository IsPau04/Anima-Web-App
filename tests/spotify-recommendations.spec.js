const { test, expect } = require('@playwright/test');

test.describe('Spotify (dummy)', () => {
  test('carga inicial recomendaciones', async () => {
    expect(true).toBe(true);
  });

  test('actualizar recomendaciones por mood', async () => {
    expect(true).toBe(true);
  });
});
